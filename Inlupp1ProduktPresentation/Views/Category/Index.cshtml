@model Inlupp1ProduktPresentation.Models.ViewModels.CategoryIndexViewModel
@{
    ViewData["Title"] = @Model.CategoryName;
}

<div class="text-center">
    <h1 class="display-4">@Model.CategoryName</h1>
    <p>Välj produkt</p>
</div>
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Produkt</th>
            <th scope="col">Beskrivning</th>
            <th scope="col">Pris</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var product in Model.Products)
    {
        <tr>
            <td><a asp-controller="Product" asp-action="Index" asp-route-Id="@product.Id">@product.Name</a></td>
                <td>@product.Description</td>
                <td>@product.Price kronor</td>
            
        </tr>
    }
    </tbody>
</table>